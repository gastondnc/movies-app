import { ItalicPipePipe } from './italic.pipe.pipe';

describe('ItalicPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ItalicPipePipe();
    expect(pipe).toBeTruthy();
  });
});
